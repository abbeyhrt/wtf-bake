window.addEventListener('keydown', e => {
  if (e.keyCode === 13) {
    window.location.replace('/recipes/random');
  }
  if (e.keyCode === 32) {
    window.location.replace('/recipes/random');
  }
});
